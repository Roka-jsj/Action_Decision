# R73 판독계층 사전등록 (07-12 01:05 커밋 — gate JSON 생성 전 타임스탬프 증거)

게이트 본체(sim/gate_m1wsT3.py)는 **무수정 동결**(GO_CASC=0.0025, codex#13 사전등록). 아래는 판독·후속 계층 규칙.

## 1. ε̂ 범주오류 확정 (게이트 임계 상향 재론 기각)
ε̂=0.0033은 solo축·8ep@lr2e-5 풀런 재현노이즈. 게이트는 casc축·2ep@lr5e-6 고정init FT를 판정 —
ε_casc,FT ∈ [0.0002, 0.0009] 추정(경로비 5.8%·casc/solo 전달비 4점 중앙 0.5), 바 +0.0025 = 2.8~6σ.
v1/v2 동시통과 요구 금지(config효과·노이즈 교락 — 오NO-GO 배가). 독립 판정.

## 2. solo GRAY 라이더 (m1wsT3·m1wsT3b 공통, 코드 무수정·문서 계층)
verdict NO-GO의 **유일 사유가 d_solo∈[-0.001,0)** (d_casc≥자기 바 ∧ CI95하한>0 ∧ sametext≥-0.002 전부 충족)이면
06:30 판정에서 FAIL 아닌 **GRAY**: 제출 불가·ws축 사망 기록 금지·재롤은 R69 선택페널티(+0.001×(N-1))로만.
사후 재게이트·게이트 상수 사후 완화 금지.

## 3. 판독계층 4조 (게이트 이진판정 불변)
- **PASS 2단**: casc≥+0.0035 = PASS-solid(후속 판독 전가중) / casc∈[+0.0025,+0.0035) = PASS-noisy
  (제출은 동일 진행 — σ_LB=0·하방없음. 단 LB 판독을 ws축 재롤 무페널티 근거·β 재캘리브에 사용 금지.
  경계통과 시 LB 기대는 [0, 0.25×casc]로 선게시).
- **LB밴드 하한 확장**: 0.79026 + 0.25×max(0, casc-0.001) (중앙 0.36×·상한 0.50× 유지).
  '실측<하한→신뢰 반감' 트립와이어는 신규 하한식 기준. 경계통과였으면 반감 대상은 소델타 단일런 게이트 계기.
- **β 4점째**: casc≥+0.005일 때만 전가중, 미만 반가중(분모 상대오차 20~36%).
- **|casc_v1-casc_v2|** = ε_FT의 config+noise 상계 1점으로 CSV 기록. 부호일치∧차≤0.002 → 방향재현 신뢰상향 /
  부호상반 → 통과자 제출하되 밴드 상한 0.36× 캡.

## 4. v2 판정 규칙
v2 json은 +0.0035 절대분기만 자동화. 'v1 대비 ≥+0.001 우위' 분기는 06:30 두 json의 d_casc 수동 비교로만.
FGM 플래그 불일치(폴백 발동 ∧ v1 fgm=1) 시 선택규칙 무효 — v1 단독 판정, v2 참고기록.

## 5. 슬롯2-3 무효화 (SWA DEAD 실측 귀결)
SWA_paired=-0.00839로 World-(a) 판별식은 FGM_paired ≥ +0.0214로 환원 — δ 최량(-0.0183) 전체 초과 요구 = 물리 모순.
① FGM_paired < +0.018(사실상 확정): 슬롯2-3 공식 무효·미사용. 오전 GPU0 = 지식 프로그램. swa2 탑재 콤보 발진 금지.
② FGM_paired ≥ +0.018(이상치): 발사 아닌 계기감사 선행(독립 재실행·ε̂ 배수·클래스별 ΔF1 분해).
   통과 시 개정 콤보 = m1t3full_fgm1_s777(SWA_K=0), 게이트 = 은행바 4조건(컷바 아님), LB밴드 중앙 +0.0006 [-0.0012,+0.0024].
③ include_fgm 임계 +0.002는 FT 미러링 전용 — 콤보 판별(+0.018)과 별개.
④ P(top-12) 갱신: 2~5%.

## 6. soup 사전등록 (wise: warm-start 보간)
wise(α) := α·raw_m1wsT3 ⊕ (1-α)·warmstart_m1, α∈{0.25,0.5,0.75} 고정. raw 공간(id_map assert), fp32 누적→fp16→q8→동일 게이트.
member_/q8dir 보간 금지. 3α = 동일풀 추가후보(R69 페널티):
(a) v1·v2 모두 NO-GO 시: 승자(argmax casc 사전고정)가 casc≥+0.0055 ∧ 나머지 3조건 통과일 때만 슬롯1
(b) GO 존재 시: 현 승자 casc+0.003 이상만 교체 (c) v1⊗v2 soup은 둘 다 GO일 때만(+0.001 가산).
슬롯1 점유 정확히 1개. 실행은 v1 게이트 후 유휴 GPU0, 06:35 디스패치에 양보.

## 7. ε̂_FT 계기런(m1wsT3e, 여유 시)
v1 env 전항 복사(fgm 실현값 미러)·q8·동일 게이트 → work/eps_ft.json.
판독: ε̂_FT_casc ≤ 0.001 → 단일런 게이트 신뢰 유지 / > 0.001 → casc<0.0035 PASS는 약한 GO 강등 +
v1·v2 교차일치(부호일치∧차≤2×ε̂_FT_casc)를 슬롯1 필요조건화. 계기는 강등·유지만(FAIL→PASS 승격 금지).
m1wsT3e 아티팩트는 제출후보 영구 금지. eps_ft.json 부재 시 교차일치 규칙만으로 06:30 진행.

## 8. FGM 플래그 기본안
양 체인 FGMFLAG는 eval_fgm_paired(+0.002) 판독-작동 분리 원칙 하에 코드 그대로 두되, codex#15가
"FGMFLAG=0 고정" 또는 "임계 +0.0035 상향"을 선택하면 그 결정이 우선(마감 03:00, FGM런 완주 전).

## 9. τ 정오표 (10:30 판정문서 기입 의무)
τ(+0.00422/+0.00162)는 처치군 h12서빙 측정치 — h8서빙 후보의 직접 추정치가 아님(보수 캐비앗 유지).
